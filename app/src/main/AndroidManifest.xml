<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.triwaluyo07.bisawicarasibi">

    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/gb_logo"
        android:label="@string/app_name"
        android:roundIcon="@drawable/gb_logo"
        android:supportsRtl="true"
        android:theme="@style/Theme.BisaWicaraSIBI"
        tools:ignore="AllowBackup">
        <activity
            android:name=".activity.LaporBugActivity"
            android:exported="true" />

        <activity
            android:name=".activity.SplashScreenActivity"
            android:theme="@style/Theme.BisaWicaraSIBI.NoActionBar"
            android:exported="true" >
        <intent-filter>
            <action android:name="android.intent.action.MAIN" />

            <category android:name="android.intent.category.LAUNCHER" />
        </intent-filter>
        </activity>

        <activity
            android:name=".activity.KamusActivity"
            android:exported="true" />
        <activity
            android:name=".activity.TestActivity"
            android:exported="true" />
        <activity
            android:name=".activity.MainActivity"
            android:theme="@style/Theme.BisaWicaraSIBI.NoActionBar"
            android:exported="true"/>

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>